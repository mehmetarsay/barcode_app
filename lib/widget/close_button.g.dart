// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'close_button.dart';

// **************************************************************************
// FunctionalWidgetGenerator
// **************************************************************************

class XButton extends StatelessWidget {
  const XButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _xButton();
}

class GoBackButton extends StatelessWidget {
  const GoBackButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _goBackButton();
}

class TitleBar extends StatelessWidget {
  const TitleBar(
    this.title, {
    Key? key,
    this.leading,
  }) : super(key: key);

  final String title;

  final Leading? leading;

  @override
  Widget build(BuildContext _context) => _titleBar(
        title,
        leading: leading,
      );
}
